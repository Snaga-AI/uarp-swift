// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import Foundation

/// Canvas documents attached to a session
public struct CanvasAPI: Sendable {
    let client: UARPClient

    init(client: UARPClient) { self.client = client }

    /// Read the canvas layout
    ///
    /// An empty canvas answers 200 with empty collections rather than 404 — there is always a
    /// canvas, it is just blank.
    ///
    /// `GET /api/v1/canvas/fleet`
    public func getFleetLayout(options: RequestOptions = .init()) async throws -> FleetLayout {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/canvas/fleet",
            options: options
        ))
    }

    /// Turn a drawn squad into a real one
    ///
    /// Upserts the supervisor and the agents wired to it into an actual Squad, so the drawing
    /// becomes something that can run and delegate. `created` says whether a squad was made or an
    /// existing one was updated.
    ///
    /// Omit `worker_ids` and the workers are taken from the PERSISTED layout's outgoing edges for
    /// that supervisor — which is not necessarily what is on the operator's screen if the canvas
    /// has unsaved changes. Send the ids to be sure.
    ///
    /// `POST /api/v1/canvas/fleet/materialize`
    public func materializeCanvasSquad(body: MaterializeCanvasSquadRequest, options: RequestOptions = .init()) async throws -> MaterializeCanvasSquadResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/canvas/fleet/materialize",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Run a loop container as a mission
    ///
    /// A loop drawn on the canvas becomes a single-objective mission that iterates until its
    /// condition verifies or a cap is hit. The caller sends the supervisor and the workers it
    /// contains; the loop's run configuration — passes, budget, minutes — is read from the
    /// **persisted** layout, which is the source of truth for it.
    ///
    /// The caps are echoed back so the client shows the limits the run is actually bound by rather
    /// than the ones on screen. Requires MEF.
    ///
    /// `POST /api/v1/canvas/loop/run`
    public func runCanvasLoop(body: RunCanvasLoopRequest, options: RequestOptions = .init()) async throws -> RunCanvasLoopResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/canvas/loop/run",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Run the wired graph as a mission
    ///
    /// The agent graph maps onto a multi-objective mission: one objective per agent step, edges
    /// become dependencies, and work advances along them. Each objective is assigned to a single
    /// agent, not a squad.
    ///
    /// A graph with no workflow edges is **400**, not an empty run: a canvas of unconnected agents
    /// has no order to execute in, and starting one would look like success. Requires MEF.
    ///
    /// `POST /api/v1/canvas/workflow/run`
    public func runCanvasWorkflow(body: RunCanvasWorkflowRequest, options: RequestOptions = .init()) async throws -> RunCanvasWorkflowResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/canvas/workflow/run",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Save the canvas layout
    ///
    /// Whole-record write under a compare-and-set: a concurrent edit answers **409** rather than
    /// overwriting the other operator's canvas.
    ///
    /// **Read `dropped` on the way back.** Entries beyond the caps (500 nodes, 1000 edges, 100
    /// notes) or failing validation are discarded, and the save still succeeds — without that field
    /// a 200 read as “saved” while the operator's cards snapped back on the next reload. An
    /// oversize body is a clean 422 instead of a silent truncation at the storage limit.
    ///
    /// `PUT /api/v1/canvas/fleet`
    public func saveFleetLayout(body: FleetLayoutUpdate, options: RequestOptions = .init()) async throws -> FleetLayout {
        return try await client.send(RequestSpec(
            method: "PUT",
            path: "/api/v1/canvas/fleet",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Run the graph on a cron
    ///
    /// Persists a trigger so the wired graph runs on a cadence. The cron expression is validated up
    /// front — an invalid one is 400 rather than a schedule that never fires. Requires MEF: without
    /// it the answer is 400, because a trigger with no engine behind it would sit there looking
    /// scheduled.
    ///
    /// `POST /api/v1/canvas/workflow/schedule`
    public func scheduleCanvasWorkflow(body: ScheduleCanvasWorkflowRequest, options: RequestOptions = .init()) async throws -> ScheduleCanvasWorkflowResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/canvas/workflow/schedule",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Remove a workflow schedule
    ///
    /// Idempotent: removing a trigger that is not there still answers 200 with `status: removed`.
    ///
    /// `DELETE /api/v1/canvas/workflow/schedule/{triggerId}`
    public func unscheduleCanvasWorkflow(triggerId: String, options: RequestOptions = .init()) async throws -> UnscheduleCanvasWorkflowResponse {
        return try await client.send(RequestSpec(
            method: "DELETE",
            path: "/api/v1/canvas/workflow/schedule/\(encodePathSegment(triggerId))",
            idempotent: true,
            options: options
        ))
    }
}

extension UARPClient {
    /// Canvas documents attached to a session
    public var canvas: CanvasAPI { CanvasAPI(client: self) }
}

// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import Foundation

/// Agent-to-Agent protocol: discovery and task execution
public struct A2AAPI: Sendable {
    let client: UARPClient

    init(client: UARPClient) { self.client = client }

    /// A2A JSON-RPC 2.0 endpoint
    ///
    /// Handles `tasks/send`, `tasks/sendSubscribe`, `tasks/get`, `tasks/cancel`,
    /// `tasks/pushNotification/set`, `tasks/pushNotification/get` via JSON-RPC 2.0.
    ///
    /// **Scope dispatch is dynamic per JSON-RPC method**: read-style methods (`tasks/get`,
    /// `tasks/pushNotification/get`) require `agents:read`; write-style (`tasks/send`,
    /// `tasks/sendSubscribe`, `tasks/cancel`, `tasks/pushNotification/set`) require `agents:write`.
    /// The static `bearerAuth: [agents:write]` declared here is the *strictest* scope; an
    /// `agents:read`-only key works for the read methods but the spec cannot express the per-method
    /// conditional.
    ///
    /// `POST /api/v1/a2a`
    ///
    /// Required scopes: `agents:write`.
    public func a2aJSONRpc(body: A2ajsonRpcRequest, options: RequestOptions = .init()) async throws -> JSONValue {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/a2a",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Cancel an A2A task
    ///
    /// `POST /api/v1/a2a/tasks/{taskId}/cancel`
    ///
    /// Required scopes: `agents:write`.
    public func cancelA2ATask(taskId: String, options: RequestOptions = .init()) async throws -> JSONValue {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/a2a/tasks/\(encodePathSegment(taskId))/cancel",
            idempotent: true,
            options: options
        ))
    }

    /// Create an A2A task
    ///
    /// Creates a new agent-to-agent task and schedules the underlying run.
    ///
    /// `POST /api/v1/a2a/tasks`
    ///
    /// Required scopes: `agents:write`.
    public func createA2ATask(body: CreateA2ATaskRequest, options: RequestOptions = .init()) async throws -> JSONValue {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/a2a/tasks",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Get A2A task status
    ///
    /// `GET /api/v1/a2a/tasks/{taskId}`
    ///
    /// Required scopes: `agents:read`.
    public func getA2ATask(taskId: String, options: RequestOptions = .init()) async throws -> JSONValue {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/a2a/tasks/\(encodePathSegment(taskId))",
            options: options
        ))
    }

    /// Get A2A agent card for discovery
    ///
    /// `GET /.well-known/agent.json`
    public func getAgentCard(agentId: String, options: RequestOptions = .init()) async throws -> JSONValue {
        var query: [URLQueryItem] = []
        query.append(URLQueryItem(name: "agent_id", value: agentId))
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/.well-known/agent.json",
            query: query,
            options: options
        ))
    }

    /// List A2A tasks
    ///
    /// `GET /api/v1/a2a/tasks`
    ///
    /// Required scopes: `agents:read`.
    public func listA2ATasks(limit: Int? = nil, cursor: String? = nil, options: RequestOptions = .init()) async throws -> ListA2ATasksResponse {
        var query: [URLQueryItem] = []
        if let limit {
            query.append(URLQueryItem(name: "limit", value: String(limit)))
        }
        if let cursor {
            query.append(URLQueryItem(name: "cursor", value: cursor))
        }
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/a2a/tasks",
            query: query,
            options: options
        ))
    }

    /// Stream every item returned by `listA2ATasks`, following the `cursor` cursor until the server
    /// reports no further pages.
    public func listA2ATasksAll(limit: Int? = nil, cursor: String? = nil, options: RequestOptions = .init()) -> AsyncThrowingStream<A2ATask, Error> {
        autoPaginate(
            fetch: { cursor in try await self.listA2ATasks(limit: limit, cursor: cursor, options: options) },
            items: { $0.tasks },
            cursor: { $0.cursor },
            hasMore: { $0.hasMore }
        )
    }

    /// Stream A2A task status updates (SSE)
    ///
    /// `GET /api/v1/a2a/tasks/{taskId}/events`
    ///
    /// Required scopes: `agents:read`.
    ///
    /// Returns a server-sent event stream; iterate it with `for try await`.
    public func streamA2ATaskEvents(taskId: String, options: RequestOptions = .init()) -> EventStream {
        return client.sendStream(RequestSpec(
            method: "GET",
            path: "/api/v1/a2a/tasks/\(encodePathSegment(taskId))/events",
            options: options
        ))
    }
}

extension UARPClient {
    /// Agent-to-Agent protocol: discovery and task execution
    public var a2a: A2AAPI { A2AAPI(client: self) }
}

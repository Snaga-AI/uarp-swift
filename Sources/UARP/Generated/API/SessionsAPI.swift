// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import Foundation

/// Conversation sessions
public struct SessionsAPI: Sendable {
    let client: UARPClient

    init(client: UARPClient) { self.client = client }

    /// Switch active branch
    ///
    /// `PUT /api/v1/sessions/{sessionId}/branches/{branchId}/activate`
    ///
    /// Required scopes: `sessions:write`.
    public func activateSessionBranch(sessionId: String, branchId: String, options: RequestOptions = .init()) async throws -> Session {
        return try await client.send(RequestSpec(
            method: "PUT",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/branches/\(encodePathSegment(branchId))/activate",
            idempotent: true,
            options: options
        ))
    }

    /// Close a session
    ///
    /// `DELETE /api/v1/sessions/{sessionId}`
    ///
    /// Required scopes: `sessions:write`.
    public func closeSession(sessionId: String, options: RequestOptions = .init()) async throws -> JSONValue {
        return try await client.send(RequestSpec(
            method: "DELETE",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))",
            idempotent: true,
            options: options
        ))
    }

    /// Confirm or cancel a todo execution
    ///
    /// `POST /api/v1/sessions/{sessionId}/todos/{todoId}/confirm`
    ///
    /// Required scopes: `sessions:write`.
    public func confirmSessionTodo(sessionId: String, todoId: String, body: JSONObject? = nil, options: RequestOptions = .init()) async throws -> JSONObject {
        let encodedBody: RequestBody? = try body.map { try client.encode($0) }
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/todos/\(encodePathSegment(todoId))/confirm",
            body: encodedBody,
            idempotent: true,
            options: options
        ))
    }

    /// Create a session
    ///
    /// `POST /api/v1/sessions`
    ///
    /// Required scopes: `sessions:write`.
    public func create(body: CreateSessionRequest, options: RequestOptions = .init()) async throws -> Session {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/sessions",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Create annotation
    ///
    /// `POST /api/v1/sessions/{sessionId}/annotations`
    ///
    /// Required scopes: `sessions:write`.
    public func createSessionAnnotation(sessionId: String, body: CreateSessionAnnotationRequest? = nil, options: RequestOptions = .init()) async throws -> CreateSessionAnnotationResponse {
        let encodedBody: RequestBody? = try body.map { try client.encode($0) }
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/annotations",
            body: encodedBody,
            idempotent: true,
            options: options
        ))
    }

    /// Create a branch point in session
    ///
    /// `POST /api/v1/sessions/{sessionId}/branch`
    ///
    /// Required scopes: `sessions:write`.
    public func createSessionBranch(sessionId: String, body: JSONObject? = nil, options: RequestOptions = .init()) async throws -> JSONObject {
        let encodedBody: RequestBody? = try body.map { try client.encode($0) }
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/branch",
            body: encodedBody,
            idempotent: true,
            options: options
        ))
    }

    /// Create session share link
    ///
    /// `POST /api/v1/sessions/{sessionId}/share`
    ///
    /// Required scopes: `sessions:write`.
    public func createSessionShare(sessionId: String, body: CreateSessionShareRequest? = nil, options: RequestOptions = .init()) async throws -> CreateSessionShareResponse {
        let encodedBody: RequestBody? = try body.map { try client.encode($0) }
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/share",
            body: encodedBody,
            idempotent: true,
            options: options
        ))
    }

    /// Create a todo in session
    ///
    /// `POST /api/v1/sessions/{sessionId}/todos`
    ///
    /// Required scopes: `sessions:write`.
    public func createSessionTodo(sessionId: String, body: CreateSessionTodoRequest, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/todos",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Create a standalone task
    ///
    /// Addresses one agent, several agents (fan-out, one session each, shared parent_task_id), or a
    /// squad. The server bootstraps the session(s). `due_at` omitted fires immediately; a timestamp
    /// schedules it; explicit `null` files it in the backlog with no schedule at all.
    ///
    /// `POST /api/v1/todos`
    public func createTask(body: CreateTaskRequest, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/todos",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Delete annotation
    ///
    /// `DELETE /api/v1/sessions/{sessionId}/annotations/{annotationId}`
    ///
    /// Required scopes: `sessions:write`.
    public func deleteSessionAnnotation(sessionId: String, annotationId: String, options: RequestOptions = .init()) async throws {
        try await client.sendVoid(RequestSpec(
            method: "DELETE",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/annotations/\(encodePathSegment(annotationId))",
            idempotent: true,
            options: options
        ))
    }

    /// Delete a single todo
    ///
    /// Deletes ONE todo and its background schedule row. The session and its other todos are
    /// untouched.
    ///
    /// `DELETE /api/v1/sessions/{sessionId}/todos/{todoId}`
    ///
    /// Required scopes: `sessions:write`.
    public func deleteSessionTodo(sessionId: String, todoId: String, options: RequestOptions = .init()) async throws -> DeleteSessionTodoResponse {
        return try await client.send(RequestSpec(
            method: "DELETE",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/todos/\(encodePathSegment(todoId))",
            idempotent: true,
            options: options
        ))
    }

    /// Get a session
    ///
    /// `GET /api/v1/sessions/{sessionId}`
    ///
    /// Required scopes: `sessions:read`.
    public func get(sessionId: String, options: RequestOptions = .init()) async throws -> JSONValue {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))",
            options: options
        ))
    }

    /// Get audit log scoped to session
    ///
    /// `GET /api/v1/sessions/{sessionId}/audit-log`
    ///
    /// Required scopes: `sessions:read`.
    public func getSessionAuditLog(sessionId: String, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/audit-log",
            options: options
        ))
    }

    /// Get feedback for a run in session
    ///
    /// `GET /api/v1/sessions/{sessionId}/runs/{runId}/feedback`
    ///
    /// Required scopes: `sessions:read`.
    public func getSessionRunFeedback(sessionId: String, runId: String, messageId: String? = nil, options: RequestOptions = .init()) async throws -> JSONObject {
        var query: [URLQueryItem] = []
        if let messageId {
            query.append(URLQueryItem(name: "message_id", value: messageId))
        }
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/runs/\(encodePathSegment(runId))/feedback",
            query: query,
            options: options
        ))
    }

    /// Get session share link status
    ///
    /// `GET /api/v1/sessions/{sessionId}/share`
    ///
    /// Required scopes: `sessions:read`.
    public func getSessionShare(sessionId: String, options: RequestOptions = .init()) async throws -> GetSessionShareResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/share",
            options: options
        ))
    }

    /// List sessions
    ///
    /// `GET /api/v1/sessions`
    ///
    /// Required scopes: `sessions:read`.
    public func list(agentId: String? = nil, limit: Int? = nil, cursor: String? = nil, options: RequestOptions = .init()) async throws -> ListSessionsResponse {
        var query: [URLQueryItem] = []
        if let agentId {
            query.append(URLQueryItem(name: "agent_id", value: agentId))
        }
        if let limit {
            query.append(URLQueryItem(name: "limit", value: String(limit)))
        }
        if let cursor {
            query.append(URLQueryItem(name: "cursor", value: cursor))
        }
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions",
            query: query,
            options: options
        ))
    }

    /// Stream every item returned by `listSessions`, following the `cursor` cursor until the server
    /// reports no further pages.
    public func listAll(agentId: String? = nil, limit: Int? = nil, cursor: String? = nil, options: RequestOptions = .init()) -> AsyncThrowingStream<ListSessionsResponseItem, Error> {
        autoPaginate(
            fetch: { cursor in try await self.list(agentId: agentId, limit: limit, cursor: cursor, options: options) },
            items: { $0.items },
            cursor: { $0.cursor },
            hasMore: { $0.hasMore }
        )
    }

    /// List session annotations
    ///
    /// `GET /api/v1/sessions/{sessionId}/annotations`
    ///
    /// Required scopes: `sessions:read`.
    public func listSessionAnnotations(sessionId: String, options: RequestOptions = .init()) async throws -> ListSessionAnnotationsResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/annotations",
            options: options
        ))
    }

    /// List artifacts across all runs in session
    ///
    /// `GET /api/v1/sessions/{sessionId}/artifacts`
    ///
    /// Required scopes: `sessions:read`.
    public func listSessionArtifacts(sessionId: String, options: RequestOptions = .init()) async throws -> ListSessionArtifactsResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/artifacts",
            options: options
        ))
    }

    /// List session branches
    ///
    /// `GET /api/v1/sessions/{sessionId}/branches`
    ///
    /// Required scopes: `sessions:read`.
    public func listSessionBranches(sessionId: String, options: RequestOptions = .init()) async throws -> ListSessionBranchesResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/branches",
            options: options
        ))
    }

    /// List session todos
    ///
    /// `GET /api/v1/sessions/{sessionId}/todos`
    ///
    /// Required scopes: `sessions:read`.
    public func listSessionTodos(sessionId: String, from: String? = nil, to: String? = nil, options: RequestOptions = .init()) async throws -> ListSessionTodosResponse {
        var query: [URLQueryItem] = []
        if let from {
            query.append(URLQueryItem(name: "from", value: from))
        }
        if let to {
            query.append(URLQueryItem(name: "to", value: to))
        }
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/todos",
            query: query,
            options: options
        ))
    }

    /// List all todos across sessions
    ///
    /// `GET /api/v1/todos`
    public func listTodos(options: RequestOptions = .init()) async throws -> ListTodosResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/todos",
            options: options
        ))
    }

    /// Resolve shared session (no auth)
    ///
    /// `GET /api/v1/shared/{shareId}`
    public func resolveSharedSession(shareId: String, options: RequestOptions = .init()) async throws -> ResolveSharedSessionResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/shared/\(encodePathSegment(shareId))",
            options: options
        ))
    }

    /// Revoke session share link
    ///
    /// `DELETE /api/v1/sessions/{sessionId}/share`
    ///
    /// Required scopes: `sessions:write`.
    public func revokeSessionShare(sessionId: String, options: RequestOptions = .init()) async throws {
        try await client.sendVoid(RequestSpec(
            method: "DELETE",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/share",
            idempotent: true,
            options: options
        ))
    }

    /// Run a pending todo immediately
    ///
    /// Dispatches the todo's run now instead of waiting for its due time. Only `pending` /
    /// `pending_confirmation` todos can be run; anything else answers 409. A recurring todo keeps
    /// its cron (next occurrence recomputed from now); a one-shot todo's schedule entry is dropped
    /// so it cannot fire twice.
    ///
    /// `POST /api/v1/sessions/{sessionId}/todos/{todoId}/run`
    ///
    /// Required scopes: `sessions:write`.
    public func runSessionTodoNow(sessionId: String, todoId: String, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/todos/\(encodePathSegment(todoId))/run",
            idempotent: true,
            options: options
        ))
    }

    /// Send message
    ///
    /// Submits a user message to the session and schedules a run on the bound agent. Returns `202
    /// Accepted` with the new run id; subscribe to `/api/v1/runs/{runId}/events` (SSE) for
    /// streaming output.
    ///
    /// `POST /api/v1/sessions/{sessionId}/messages`
    ///
    /// Required scopes: `sessions:write`.
    public func sendSessionMessage(sessionId: String, body: SendSessionMessageRequest, options: RequestOptions = .init()) async throws -> SendSessionMessageResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/messages",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Save feedback/reaction for a session run
    ///
    /// `PUT /api/v1/sessions/{sessionId}/runs/{runId}/feedback`
    ///
    /// Required scopes: `sessions:write`.
    public func setSessionRunFeedback(sessionId: String, runId: String, body: JSONObject, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "PUT",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/runs/\(encodePathSegment(runId))/feedback",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Stream session events (SSE)
    ///
    /// `GET /api/v1/sessions/{sessionId}/events`
    ///
    /// Required scopes: `events:read`.
    ///
    /// Returns a server-sent event stream; iterate it with `for try await`.
    public func streamSessionEvents(sessionId: String, token: String? = nil, lastEventId: String? = nil, options: RequestOptions = .init()) -> EventStream {
        var query: [URLQueryItem] = []
        if let token {
            query.append(URLQueryItem(name: "token", value: token))
        }
        var headers: [String: String] = [:]
        if let lastEventId {
            headers["Last-Event-ID"] = lastEventId
        }
        return client.sendStream(RequestSpec(
            method: "GET",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/events",
            query: query,
            headers: headers,
            options: options
        ))
    }

    /// Update session metadata
    ///
    /// `PUT /api/v1/sessions/{sessionId}`
    ///
    /// Required scopes: `sessions:write`.
    public func update(sessionId: String, body: UpdateSessionRequest, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "PUT",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Update annotation (e.g. resolve)
    ///
    /// `PATCH /api/v1/sessions/{sessionId}/annotations/{annotationId}`
    ///
    /// Required scopes: `sessions:write`.
    public func updateSessionAnnotation(sessionId: String, annotationId: String, body: UpdateSessionAnnotationRequest? = nil, options: RequestOptions = .init()) async throws -> JSONValue {
        let encodedBody: RequestBody? = try body.map { try client.encode($0) }
        return try await client.send(RequestSpec(
            method: "PATCH",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/annotations/\(encodePathSegment(annotationId))",
            body: encodedBody,
            idempotent: true,
            options: options
        ))
    }

    /// Update a todo
    ///
    /// `PATCH /api/v1/sessions/{sessionId}/todos/{todoId}`
    ///
    /// Required scopes: `sessions:write`.
    public func updateSessionTodo(sessionId: String, todoId: String, body: JSONObject, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "PATCH",
            path: "/api/v1/sessions/\(encodePathSegment(sessionId))/todos/\(encodePathSegment(todoId))",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }
}

extension UARPClient {
    /// Conversation sessions
    public var sessions: SessionsAPI { SessionsAPI(client: self) }
}

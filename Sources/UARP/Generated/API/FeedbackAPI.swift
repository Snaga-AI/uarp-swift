// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import Foundation

/// Error reports from any tenant; the inbox is super-admin only
public struct FeedbackAPI: Sendable {
    let client: UARPClient

    init(client: UARPClient) { self.client = client }

    /// Report an error or send feedback
    ///
    /// Any authenticated caller. `message` is required; everything else is optional and clipped to
    /// a maximum length rather than rejected, so a long paste still files a report instead of
    /// losing it.
    ///
    /// `POST /api/v1/feedback`
    public func submitFeedback(body: SubmitFeedbackRequest, options: RequestOptions = .init()) async throws -> SubmitFeedbackResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/feedback",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }
}

extension UARPClient {
    /// Error reports from any tenant; the inbox is super-admin only
    public var feedback: FeedbackAPI { FeedbackAPI(client: self) }
}

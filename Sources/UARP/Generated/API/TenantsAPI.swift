// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import Foundation

/// Tenant management and API keys
public struct TenantsAPI: Sendable {
    let client: UARPClient

    init(client: UARPClient) { self.client = client }

    /// Create a new API key
    ///
    /// `POST /api/v1/tenants/me/keys`
    ///
    /// Required scopes: `tenants:write`.
    public func createAPIKey(body: CreateAPIKeyRequest, options: RequestOptions = .init()) async throws -> APIKeyResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/tenants/me/keys",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Create a new tenant with the caller as owner
    ///
    /// Provisions a brand-new tenant on the `free` plan (or `enterprise` for super-admin). The
    /// caller becomes the founding owner. Onboarding wizard auto-runs on /browser to provision the
    /// Head Agent.
    ///
    /// `POST /api/v1/me/tenants/create`
    public func createMyTenant(body: CreateMyTenantRequest, options: RequestOptions = .init()) async throws -> CreateMyTenantResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/me/tenants/create",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Get current tenant
    ///
    /// Returns the calling tenant's profile. Any authenticated key passes — no scope required.
    ///
    /// `GET /api/v1/tenants/me`
    ///
    /// Required scopes: `api_keys:read`.
    public func getCurrentTenant(options: RequestOptions = .init()) async throws -> Tenant {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/tenants/me",
            options: options
        ))
    }

    /// Tiered Head Agent template for the onboarding wizard
    ///
    /// Returns Basic/Standard/Full tier metadata for the current tenant under the Variant-A
    /// SPEC-driven contract. Each tier names the `@platform/*` SPECs the wizard installs (via
    /// `install_specs`) plus an `auto_approve_tools` HITL list. Availability is a plan-rank check
    /// (`required_plan`); locked tiers ship empty `install_specs`.
    ///
    /// `GET /api/v1/me/head-agent-template`
    public func getMyHeadAgentTemplate(options: RequestOptions = .init()) async throws -> GetMyHeadAgentTemplateResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/me/head-agent-template",
            options: options
        ))
    }

    /// List API keys
    ///
    /// `GET /api/v1/tenants/me/keys`
    ///
    /// Required scopes: `api_keys:read`.
    public func listAPIKeys(options: RequestOptions = .init()) async throws -> ListAPIKeysResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/tenants/me/keys",
            options: options
        ))
    }

    /// List the caller's tenant memberships + pending invites
    ///
    /// Returns every tenant the calling user is a member of, plus any pending invites addressed to
    /// their email. Used by the Netflix-style picker (/browser/tenants).
    ///
    /// `GET /api/v1/me/tenants`
    public func listMyTenants(options: RequestOptions = .init()) async throws -> ListMyTenantsResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/me/tenants",
            options: options
        ))
    }

    /// Find which tenant owns the given agent across caller's memberships
    ///
    /// Used by the chat layout fallback when the URL points at an agent under a different active
    /// tenant. Returns the owning tenant_id in one round-trip; the alternative was N client-side
    /// probes that surfaced expected 404s in the browser console.
    ///
    /// `GET /api/v1/me/agents/{agentId}/locate`
    public func locateMyAgent(agentId: String, options: RequestOptions = .init()) async throws -> LocateMyAgentResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/me/agents/\(encodePathSegment(agentId))/locate",
            options: options
        ))
    }

    /// Partial update tenant
    ///
    /// Partial update of tenant settings. Requires the `tenants:write` scope and role `owner`.
    ///
    /// `PATCH /api/v1/tenants/me`
    ///
    /// Required scopes: `tenants:write`.
    public func patch(body: JSONObject, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "PATCH",
            path: "/api/v1/tenants/me",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Revoke an API key
    ///
    /// `DELETE /api/v1/tenants/me/keys/{keyId}`
    ///
    /// Required scopes: `tenants:write`.
    public func revokeAPIKey(keyId: String, options: RequestOptions = .init()) async throws -> JSONValue {
        return try await client.send(RequestSpec(
            method: "DELETE",
            path: "/api/v1/tenants/me/keys/\(encodePathSegment(keyId))",
            idempotent: true,
            options: options
        ))
    }

    /// Update current tenant settings
    ///
    /// `PUT /api/v1/tenants/me`
    ///
    /// Required scopes: `tenants:write`.
    public func update(body: UpdateTenantRequest? = nil, options: RequestOptions = .init()) async throws -> JSONValue {
        let encodedBody: RequestBody? = try body.map { try client.encode($0) }
        return try await client.send(RequestSpec(
            method: "PUT",
            path: "/api/v1/tenants/me",
            body: encodedBody,
            idempotent: true,
            options: options
        ))
    }

    /// Verify domain
    ///
    /// `POST /api/v1/tenants/me/domain/verify`
    ///
    /// Required scopes: `tenants:write`.
    public func verifyTenantDomain(body: JSONObject, options: RequestOptions = .init()) async throws -> VerifyTenantDomainResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/tenants/me/domain/verify",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }
}

extension UARPClient {
    /// Tenant management and API keys
    public var tenants: TenantsAPI { TenantsAPI(client: self) }
}
